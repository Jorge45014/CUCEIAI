<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.CUCEIAI" parent="Base.Theme.CUCEIAI">
        <!-- Transparent system bars for edge-to-edge. -->
        <!-- Status Bar (barra superior) -->
        <item name="colorPrimaryDark">@color/black</item>

        <!-- Color de acento (para botones, checkboxes, etc.) -->
        <item name="colorAccent">@color/black</item>

        <!-- Otros colores del tema -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorOnPrimary">@color/black</item>
        <item name="colorSurface">@color/surface</item>

        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">?attr/isLightTheme</item>
    </style>

    <!-- Estilo personalizado para AlertDialog -->
    <style name="CustomAlertDialogTheme" parent="Theme.AppCompat.Dialog.Alert">
        <item name="android:windowBackground">@drawable/rounded_dialog</item>
        <!-- Color texto principal -->
        <item name="android:textColorPrimary">#000000</item>
        <!-- Color texto secundario -->
        <item name="android:textColorSecondary">#000000</item>
        <!-- Color de acento (botones) -->
        <item name="colorAccent">#4CAF50</item>
        <!-- Color texto botones -->
        <item name="buttonBarButtonStyle">@style/AlertDialogButtons</item>
        <item name="buttonBarPositiveButtonStyle">@style/PositiveButton</item>
        <item name="buttonBarNegativeButtonStyle">@style/NegativeButton</item>
    </style>

    <style name="AlertDialogTitle" parent="TextAppearance.AppCompat.Title">
        <item name="android:textColor">#000000</item>
    </style>

    <style name="AlertDialogBody" parent="TextAppearance.AppCompat.Body1">
        <item name="android:textColor">#000000</item>
    </style>

    <style name="AlertDialogButtons" parent="Widget.AppCompat.Button.ButtonBar.AlertDialog">
        <item name="android:textColor">#000000</item>
    </style>

    <style name="PositiveButton" parent="Widget.AppCompat.Button.ButtonBar.AlertDialog">
        <item name="android:textColor">#4CAF50</item> <!-- Verde -->
    </style>

    <style name="NegativeButton" parent="Widget.AppCompat.Button.ButtonBar.AlertDialog">
        <item name="android:textColor">#F44336</item> <!-- Rojo -->
    </style>
</resources>